events {
    worker_connections 1024;
}

http {
    # MIME types
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Confiar en Cloudflare IPs
    set_real_ip_from 173.245.48.0/20;
    set_real_ip_from 103.21.244.0/22;
    set_real_ip_from 103.22.200.0/22;
    set_real_ip_from 103.31.4.0/22;
    set_real_ip_from 141.101.64.0/18;
    set_real_ip_from 108.162.192.0/18;
    set_real_ip_from 190.93.240.0/20;
    set_real_ip_from 188.114.96.0/20;
    set_real_ip_from 197.234.240.0/22;
    set_real_ip_from 198.41.128.0/17;
    set_real_ip_from 162.158.0.0/15;
    set_real_ip_from 104.16.0.0/13;
    set_real_ip_from 104.24.0.0/14;
    set_real_ip_from 172.64.0.0/13;
    set_real_ip_from 131.0.72.0/22;
    real_ip_header CF-Connecting-IP;

    upstream auth_backend {
        server auth-service:8000;
    }

    upstream fastfood_backend {
        server fast-food-service:8000;
    }

    upstream frontend_app {
        server frontend:3000;
    }

    upstream restaurant_backend {
        server restaurant-service:8000;
    }

    upstream hotel_backend {
        server hotel-service:8000;
    }

    server {
        listen 80;
        server_name aurora.fronteratech.ec 146.190.217.68;
        client_max_body_size 100M;

        # Django Admin para Fast-Food
        location /fast-food/admin {
            proxy_pass http://fastfood_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        # Static files para fast-food (admin)
        location /fast-food/static/ {
            alias /usr/share/nginx/html/static/fastfood/;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }

        # MEDIA FILES - Imágenes de productos
        location /media/ {
            alias /usr/share/nginx/html/media/;
            expires 30d;
            add_header Cache-Control "public, immutable";
            add_header Access-Control-Allow-Origin *;
        }

        # Auth Authentication API
        location /api/authentication/ {
            rewrite ^/api/authentication/(.*) /auth/api/authentication/$1 break;
            proxy_pass http://auth_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        # Auth Users API
        location /api/users/ {
            rewrite ^/api/users/(.*) /auth/api/users/$1 break;
            proxy_pass http://auth_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        # Auth Roles API
        location /api/roles/ {
            rewrite ^/api/roles/(.*) /auth/api/roles/$1 break;
            proxy_pass http://auth_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        # Agente fast-food (fix URL duplicada)
        location ^~ /api/fast-food/api/ {
            proxy_pass http://fastfood_backend/fast-food/api/;
            proxy_set_header Host fast-food-service:8000;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Fast Food Service API
        location /api/fast-food/ {
            rewrite ^/api/fast-food/(.*) /fast-food/api/$1 break;
            proxy_pass http://fastfood_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        # Static files para auth
        location /static/auth/ {
            alias /usr/share/nginx/html/static/auth/;
        }

        # Static files para fast-food (legacy)
        location /static/fastfood/ {
            alias /usr/share/nginx/html/static/fastfood/;
        }

        # Agente restaurant (fix URL duplicada)
        location ^~ /api/restaurant/api/hardware/ {
            proxy_pass http://restaurant_backend/restaurant/api/hardware/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Restaurant Service API
        location /api/restaurant/ {
            rewrite ^/api/restaurant/(.*) /restaurant/api/$1 break;
            proxy_pass http://restaurant_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        # Hotel Service API
        location /api/hotel/api/ {
            proxy_pass http://hotel_backend/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        location /api/hotel/ {
            rewrite ^/api/hotel/(.*) /api/$1 break;
            proxy_pass http://hotel_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        # WebSockets for Restaurant (inventory)
        location /ws/inventory/ {
            proxy_pass http://restaurant_backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
        }

        # WebSockets for Hotel (invoice status)
        location /ws/ {
            proxy_pass http://hotel_backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 300s;
        }

        location ^~ /restaurant/admin {
            proxy_pass http://restaurant_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_read_timeout 120s;
            proxy_connect_timeout 120s;
        }

        # Static files para restaurant
        location ^~ /restaurant/static/ {
            alias /usr/share/nginx/html/static/restaurant/;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }

        location /static/restaurant/ {
            alias /usr/share/nginx/html/static/restaurant/;
        }

        # Media files para restaurant
        location ^~ /restaurant/media/ {
            alias /usr/share/nginx/html/restaurant/media/;
            expires 30d;
            add_header Cache-Control "public, immutable";
            add_header Access-Control-Allow-Origin *;
        }

        # WebSocket para React hot reload
        location /ws {
            proxy_pass http://frontend_app;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
        }

        # Frontend (AL FINAL)
        location / {
            proxy_pass http://frontend_app;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            add_header Pragma "no-cache";
            expires 0;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
        }
    }
}